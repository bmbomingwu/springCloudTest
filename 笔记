Bean注册失败：（启动失败）
1、类的路径冲突、出现在maven依赖中（子工程与父工程中出现同路径的类且有注解）
2、bean的id冲突，id为注解赋的值，不赋值默认以类的小驼峰式命名

resource与autowire区别

resource:
name与type类型都支持，没有默认选项,需指定
都不指定-----按照变量名去找，找不到系统报错。
指定name值---先按照name值寻找，找不到启动报错。
指定type值---先按照变量名去找，如果找到与指定type类型对比，不一致启动报错；
                               如果未找到，按照type类型找，找不到启动报错。
如果当前类或接口有多个子类或实现类，按照类型找到，再按照变量名去查找指定注入那个bean，找不到启动报错。

autowire:
只支持type类型查找，如想用name查找需搭配@Qualifier注解
按照type查找，找不到启动报错。
如果当前类或接口有多个子类或实现类，按照类型找到，再按照变量名去查找指定注入那个bean，找不到启动报错。
加上@Qualifier注解按照name找，根据注解的值如果找不到启动报错。



以上情况找到，但与注入类不一致启动报错。


